#![allow(
    non_snake_case,
    unused_parens,
    unused_doc_comments,
    non_upper_case_globals
)]

use std::prelude::rust_2024::*;

fn main()->Result<(), std::io::Error> 
{
    println!("Hello World");

    

    Ok(())
}
