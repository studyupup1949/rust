# Skills

Skills are available on demand. Use `search_skills` with a short query to discover relevant skills, then invoke `Skill` with the selected `skill_name` when specialized instructions are useful.
