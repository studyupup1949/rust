# Birch Service Latency Benchmark

Under the same 10,000-request workload, the measured p95 request latency was
100 milliseconds before the scheduler change and 80 milliseconds after the
change. The benchmark did not measure throughput, cost, or error rate.
