name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # ───────────────────────────────────────────────
  # Gate: the exact main-branch CI workflow must pass before any publishing
  # ───────────────────────────────────────────────
  verify:
    name: Full CI
    uses: ./.github/workflows/ci.yml

  release-metadata:
    name: Release Metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify tag and release metadata
        shell: bash
        run: |
          set -euo pipefail

          if [[ ! "$GITHUB_REF_NAME" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "::error::Release tag must use vMAJOR.MINOR.PATCH, got $GITHUB_REF_NAME"
            exit 1
          fi

          version="${GITHUB_REF_NAME#v}"
          manifest_version=$(awk -F'"' '/^version = "/ { print $2; exit }' Cargo.toml)
          chart_version=$(awk '/^version:/ { print $2; exit }' deploy/helm/a3s-gateway/Chart.yaml | tr -d '"')
          app_version=$(awk '/^appVersion:/ { print $2; exit }' deploy/helm/a3s-gateway/Chart.yaml | tr -d '"')

          for pair in \
            "Cargo.toml:$manifest_version" \
            "Chart.yaml version:$chart_version" \
            "Chart.yaml appVersion:$app_version"; do
            name="${pair%%:*}"
            actual="${pair#*:}"
            if [[ "$actual" != "$version" ]]; then
              echo "::error::$name is $actual, expected $version from $GITHUB_REF_NAME"
              exit 1
            fi
          done

          if ! grep -Eq "^## \\[$version\\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$" CHANGELOG.md; then
            echo "::error::CHANGELOG.md has no dated [$version] release section"
            exit 1
          fi

  # ───────────────────────────────────────────────
  # Build cross-compiled binaries
  # ───────────────────────────────────────────────
  build:
    name: Build / ${{ matrix.target }}
    needs: [verify, release-metadata]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            platform: darwin-arm64
          - target: x86_64-apple-darwin
            os: macos-latest
            platform: darwin-x86_64
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            platform: linux-x86_64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            platform: linux-arm64
            use_cross: true
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            platform: linux-x86_64-musl
            use_zigbuild: true
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            platform: linux-arm64-musl
            use_zigbuild: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            platform: windows-x86_64
          - target: aarch64-pc-windows-msvc
            os: windows-latest
            platform: windows-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross == true
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Install cargo-zigbuild
        if: matrix.use_zigbuild == true
        run: |
          pip3 install ziglang --quiet
          cargo install cargo-zigbuild --quiet

      - name: Build
        # Linux targets enable `kube` and `redis` features for K8s Ingress + distributed rate-limiting.
        # macOS and Windows keep default features (no K8s deployment use case).
        shell: bash
        run: |
          if [[ "${{ matrix.target }}" == *"linux"* ]]; then
            FEATURES="--features kube,redis"
          else
            FEATURES=""
          fi
          if [ "${{ matrix.use_cross }}" = "true" ]; then
            cross build --locked -p a3s-gateway --release --target ${{ matrix.target }} $FEATURES
          elif [ "${{ matrix.use_zigbuild }}" = "true" ]; then
            cargo zigbuild --locked -p a3s-gateway --release --target ${{ matrix.target }} $FEATURES
          else
            cargo build --locked -p a3s-gateway --release --target ${{ matrix.target }} $FEATURES
          fi

      - name: Strip (Linux gnu)
        if: runner.os == 'Linux' && matrix.use_zigbuild != true
        run: |
          BIN="target/${{ matrix.target }}/release/a3s-gateway"
          if [ "${{ matrix.target }}" = "aarch64-unknown-linux-gnu" ]; then
            sudo apt-get install -y --no-install-recommends binutils-aarch64-linux-gnu
            aarch64-linux-gnu-strip "$BIN"
          else
            strip "$BIN"
          fi

      - name: Strip (Linux musl)
        if: matrix.use_zigbuild == true
        run: |
          sudo apt-get install -y --no-install-recommends llvm
          llvm-strip "target/${{ matrix.target }}/release/a3s-gateway"

      - name: Strip (macOS)
        if: runner.os == 'macOS'
        run: strip "target/${{ matrix.target }}/release/a3s-gateway"

      - name: Package Unix archive
        if: runner.os != 'Windows'
        shell: bash
        run: |
          BIN="target/${{ matrix.target }}/release/a3s-gateway"
          VER="${GITHUB_REF_NAME#v}"
          ARCHIVE="a3s-gateway-${VER}-${{ matrix.platform }}.tar.gz"
          tar czf "$ARCHIVE" -C "$(dirname $BIN)" a3s-gateway
          sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256"

      - name: Package Windows archive
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $binary = "target/${{ matrix.target }}/release/a3s-gateway.exe"
          $version = $env:GITHUB_REF_NAME.TrimStart('v')
          $archive = "a3s-gateway-$version-${{ matrix.platform }}.zip"
          Compress-Archive -LiteralPath $binary -DestinationPath $archive
          $sha256 = (Get-FileHash -LiteralPath $archive -Algorithm SHA256).Hash.ToLowerInvariant()
          Set-Content -LiteralPath "$archive.sha256" -Value "$sha256  $archive" -Encoding ascii

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: a3s-gateway-*

  # ───────────────────────────────────────────────
  # Create GitHub Release with binaries
  # ───────────────────────────────────────────────
  github-release:
    name: GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect assets
        run: |
          mkdir release
          find artifacts -type f \( -name '*.tar.gz' -o -name '*.zip' -o -name '*.sha256' \) \
            -exec cp {} release/ \;
          ls -lh release/

      - name: Generate release notes
        run: |
          VER="${GITHUB_REF_NAME#v}"
          # Extract the section for this version from CHANGELOG.md
          if [ -f CHANGELOG.md ]; then
            awk "/^## \[${VER}\]/{found=1; next} /^## \[/{if(found) exit} found" CHANGELOG.md > /tmp/notes.md
          fi
          # Fallback to git log if CHANGELOG section is empty
          if [ ! -s /tmp/notes.md ]; then
            PREV_TAG=$(git tag --sort=-v:refname | grep '^v' | head -2 | tail -1)
            if [ -z "$PREV_TAG" ]; then
              echo "Initial release." > /tmp/notes.md
            else
              git log "${PREV_TAG}..HEAD" --oneline --no-merges \
                --pretty=format:"- %s" | head -60 > /tmp/notes.md
            fi
          fi

      - name: Create or update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "$GITHUB_REF_NAME" &>/dev/null; then
            gh release upload "$GITHUB_REF_NAME" release/* --clobber
          else
            gh release create "$GITHUB_REF_NAME" release/* \
              --title "$GITHUB_REF_NAME" \
              --notes-file /tmp/notes.md
          fi

  # ───────────────────────────────────────────────
  # Publish to crates.io
  # ───────────────────────────────────────────────
  publish-crate:
    name: Publish to crates.io
    needs: github-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Publish dry-run
        run: cargo publish --locked --dry-run

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_TOKEN }}
        run: cargo publish --locked

  # ───────────────────────────────────────────────
  # Build and push multi-arch OCI images to GHCR
  # ───────────────────────────────────────────────
  publish-oci:
    name: Publish OCI Images
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Download musl binaries
        uses: actions/download-artifact@v4
        with:
          path: bin

      - name: Extract binaries and prepare build contexts
        shell: bash
        run: |
          mkdir -p bin/linux-amd64 bin/linux-arm64

          tar -xzf bin/x86_64-unknown-linux-musl/*.tar.gz -C bin/linux-amd64
          mv bin/linux-amd64/a3s-gateway bin/linux-amd64/a3s-gateway-amd64

          tar -xzf bin/aarch64-unknown-linux-musl/*.tar.gz -C bin/linux-arm64
          mv bin/linux-arm64/a3s-gateway bin/linux-arm64/a3s-gateway-arm64

          cp Dockerfile bin/linux-amd64/
          cp Dockerfile bin/linux-arm64/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          VER="${GITHUB_REF_NAME#v}"
          REGISTRY="ghcr.io/$(echo "$GITHUB_REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')/gateway"
          echo "VER=$VER" >> "$GITHUB_OUTPUT"
          echo "REGISTRY=$REGISTRY" >> "$GITHUB_OUTPUT"

      - name: Build and push linux/amd64
        uses: docker/build-push-action@v6
        with:
          context: bin/linux-amd64
          file: bin/linux-amd64/Dockerfile
          platforms: linux/amd64
          build-args: BINARY_NAME=a3s-gateway-amd64
          tags: ${{ steps.version.outputs.REGISTRY }}:${{ steps.version.outputs.VER }}-amd64
          push: true
          provenance: false

      - name: Build and push linux/arm64
        uses: docker/build-push-action@v6
        with:
          context: bin/linux-arm64
          file: bin/linux-arm64/Dockerfile
          platforms: linux/arm64
          build-args: BINARY_NAME=a3s-gateway-arm64
          tags: ${{ steps.version.outputs.REGISTRY }}:${{ steps.version.outputs.VER }}-arm64
          push: true
          provenance: false

      - name: Create and push multi-arch manifest
        run: |
          REGISTRY="${{ steps.version.outputs.REGISTRY }}"
          VER="${{ steps.version.outputs.VER }}"

          # Create manifest for versioned tag
          docker buildx imagetools create -t "${REGISTRY}:${VER}" \
            "${REGISTRY}:${VER}-amd64" \
            "${REGISTRY}:${VER}-arm64"

          # Create manifest for latest tag
          docker buildx imagetools create -t "${REGISTRY}:latest" \
            "${REGISTRY}:${VER}-amd64" \
            "${REGISTRY}:${VER}-arm64"

      - name: Verify multi-arch manifest
        run: |
          REGISTRY="${{ steps.version.outputs.REGISTRY }}"
          VER="${{ steps.version.outputs.VER }}"
          echo "=== ${REGISTRY}:${VER} ==="
          docker buildx imagetools inspect "${REGISTRY}:${VER}"
          echo ""
          echo "=== ${REGISTRY}:latest ==="
          docker buildx imagetools inspect "${REGISTRY}:latest"

  # ───────────────────────────────────────────────
  # Update Homebrew formula
  # ───────────────────────────────────────────────
  update-homebrew:
    name: Update Homebrew
    needs: github-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Compute sha256
        id: sha
        run: |
          set -euo pipefail
          VER="${GITHUB_REF_NAME#v}"
          sha() {
            sha256sum "artifacts/$1/a3s-gateway-${VER}-$2.tar.gz" | awk '{print $1}'
          }
          echo "ver=$VER"                                                    >> "$GITHUB_OUTPUT"
          echo "macos_arm64=$(sha aarch64-apple-darwin darwin-arm64)"        >> "$GITHUB_OUTPUT"
          echo "macos_x64=$(sha x86_64-apple-darwin darwin-x86_64)"          >> "$GITHUB_OUTPUT"
          echo "linux_arm64=$(sha aarch64-unknown-linux-musl linux-arm64-musl)" >> "$GITHUB_OUTPUT"
          echo "linux_x64=$(sha x86_64-unknown-linux-musl linux-x86_64-musl)"   >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          repository: A3S-Lab/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Regenerate formula
        working-directory: homebrew-tap
        env:
          VER: ${{ steps.sha.outputs.ver }}
          SHA_MACOS_ARM64: ${{ steps.sha.outputs.macos_arm64 }}
          SHA_MACOS_X64: ${{ steps.sha.outputs.macos_x64 }}
          SHA_LINUX_ARM64: ${{ steps.sha.outputs.linux_arm64 }}
          SHA_LINUX_X64: ${{ steps.sha.outputs.linux_x64 }}
        run: python3 ../.github/update-homebrew-formula.py

      - name: Commit and push
        working-directory: homebrew-tap
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/a3s-gateway.rb
          git diff --cached --quiet && exit 0
          git commit -m "chore: update a3s-gateway to v${{ steps.sha.outputs.ver }}"
          git push origin main
