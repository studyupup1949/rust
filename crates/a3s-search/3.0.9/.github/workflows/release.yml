name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  classify:
    name: Classify release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release.outputs.version }}
      stable: ${{ steps.release.outputs.stable }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Validate release identity
        id: release
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          package_version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          if [ "$package_version" != "$version" ]; then
            echo "Tag version ($version) does not match Cargo.toml version ($package_version)" >&2
            exit 1
          fi
          case "$version" in
            *-*) stable=false ;;
            *) stable=true ;;
          esac
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "stable=$stable" >> "$GITHUB_OUTPUT"

  ci:
    name: CI
    needs: classify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.96.0
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2 peeled commit

      - name: Wait for the independently published Browser crate
        shell: bash
        run: |
          set -euo pipefail
          requirement=$(cargo metadata --locked --no-deps --format-version 1 |
            jq -r '.packages[0].dependencies[] | select(.name == "a3s-use-browser") | .req')
          case "$requirement" in
            =?*) version="${requirement#=}" ;;
            *)
              echo "a3s-use-browser must use an exact '=x.y.z' release version" >&2
              exit 1
              ;;
          esac
          for attempt in $(seq 1 30); do
            if curl --retry 3 --retry-all-errors --retry-delay 2 -fsSL \
              -A "a3s-search-release-check/1.0" \
              "https://crates.io/api/v1/crates/a3s-use-browser/${version}" >/dev/null; then
              echo "a3s-use-browser ${version} is available"
              exit 0
            fi
            echo "Waiting for a3s-use-browser ${version} on crates.io (${attempt}/30)"
            sleep 10
          done
          echo "a3s-use-browser ${version} must be published before A3S Search" >&2
          exit 1

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: |
          cargo clippy --all-targets --no-default-features --locked -- -D warnings
          cargo clippy --all-targets --all-features --locked -- -D warnings

      - name: Tests
        run: |
          cargo test --no-default-features --locked
          cargo test --all-features --locked

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps --all-features --locked

      - name: Release archive contract
        run: scripts/test-release-package.sh

      - name: Package verification
        run: cargo package --locked

  freeze-crate:
    name: Freeze exact crate bytes
    needs: [classify, ci]
    runs-on: ubuntu-latest
    outputs:
      crate_sha256: ${{ steps.identity.outputs.crate_sha256 }}
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.96.0

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2 peeled commit

      - name: Package and freeze
        shell: bash
        run: |
          set -euo pipefail
          version="${{ needs.classify.outputs.version }}"
          cargo package --locked
          scripts/freeze-crate.sh \
            "target/package/a3s-search-${version}.crate" \
            frozen-crate \
            "$GITHUB_SHA" \
            "$GITHUB_REF"

      - name: Export frozen identities
        id: identity
        shell: bash
        run: |
          set -euo pipefail
          crate_sha256=$(jq -er '.package.sha256' frozen-crate/frozen-crate.json)
          echo "crate_sha256=${crate_sha256}" >> "$GITHUB_OUTPUT"

      - name: Upload frozen crate
        id: upload
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: frozen-crate-${{ needs.classify.outputs.version }}
          path: frozen-crate
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

  release-gate:
    name: Release gate
    if: always() && !cancelled()
    needs: [classify, ci, freeze-crate]
    runs-on: ubuntu-latest
    steps:
      - name: Require verified source and package
        env:
          CLASSIFY_RESULT: ${{ needs.classify.result }}
          CI_RESULT: ${{ needs.ci.result }}
          FROZEN_CRATE_RESULT: ${{ needs.freeze-crate.result }}
          FROZEN_CRATE_SHA256: ${{ needs.freeze-crate.outputs.crate_sha256 }}
          FROZEN_ARTIFACT_ID: ${{ needs.freeze-crate.outputs.artifact_id }}
          FROZEN_ARTIFACT_DIGEST: ${{ needs.freeze-crate.outputs.artifact_digest }}
        shell: bash
        run: |
          set -euo pipefail
          test "$CLASSIFY_RESULT" = success
          test "$CI_RESULT" = success
          test "$FROZEN_CRATE_RESULT" = success
          test -n "$FROZEN_CRATE_SHA256"
          test -n "$FROZEN_ARTIFACT_ID"
          test -n "$FROZEN_ARTIFACT_DIGEST"

  # ───────────────────────────────────────────────
  # Build CLI binary for 4 platforms
  # ───────────────────────────────────────────────
  build-cli:
    name: Build / ${{ matrix.name }}
    if: |
      always() &&
      !cancelled() &&
      needs.release-gate.result == 'success'
    needs: release-gate
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            name: darwin-arm64
          - target: x86_64-apple-darwin
            os: macos-latest
            name: darwin-x86_64
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: linux-x86_64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            name: linux-arm64
            cross: true

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.96.0
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2 peeled commit

      - name: Install cross toolchain
        if: matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      - name: Build
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Strip & Package
        shell: bash
        run: |
          BINARY="target/${{ matrix.target }}/release/a3s-search"
          STRIP=${STRIP_CMD:-strip}
          $STRIP "$BINARY" 2>/dev/null || true

          VERSION="${GITHUB_REF_NAME#v}"
          ARCHIVE="a3s-search-${VERSION}-${{ matrix.name }}.tar.gz"
          scripts/package-release.sh "$BINARY" "$ARCHIVE"
          echo "ARCHIVE=$ARCHIVE" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: cli-${{ matrix.name }}
          path: ${{ env.ARCHIVE }}

  # ───────────────────────────────────────────────
  # Publish core crate to crates.io
  # ───────────────────────────────────────────────
  publish-crate:
    name: Publish crate
    if: needs.classify.outputs.stable == 'true'
    needs: [classify, freeze-crate, release-gate]
    runs-on: ubuntu-latest
    environment: stable-release
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.96.0

      - name: Download frozen crate
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: frozen-crate-${{ needs.classify.outputs.version }}
          path: ${{ runner.temp }}/frozen-crate

      - name: Reproduce and publish the frozen package
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_TOKEN }}
          EXPECTED_CRATE_SHA256: ${{ needs.freeze-crate.outputs.crate_sha256 }}
          VERSION: ${{ needs.classify.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          expected="${EXPECTED_CRATE_SHA256#sha256:}"
          frozen="${RUNNER_TEMP}/frozen-crate/a3s-search-${VERSION}.crate"
          test "$(sha256sum "$frozen" | awk '{print $1}')" = "$expected"
          cargo package --locked
          packaged="target/package/a3s-search-${VERSION}.crate"
          test "$(sha256sum "$packaged" | awk '{print $1}')" = "$expected"
          cargo publish --locked --no-verify

  # ───────────────────────────────────────────────
  # Create GitHub Release with CLI binaries
  # ───────────────────────────────────────────────
  github-release:
    name: GitHub Release
    if: |
      always() &&
      !cancelled() &&
      needs.classify.result == 'success' &&
      needs.release-gate.result == 'success' &&
      needs.build-cli.result == 'success' &&
      (
        (needs.classify.outputs.stable == 'true' &&
          needs.publish-crate.result == 'success') ||
        (needs.classify.outputs.stable == 'false' &&
          needs.publish-crate.result == 'skipped')
      )
    needs: [classify, release-gate, publish-crate, build-cli]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Download artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: cli-*
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum *.tar.gz > checksums.txt
          cat checksums.txt

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate release notes
        id: notes
        run: |
          PREV_TAG=$(git tag --sort=-v:refname | grep '^v' | head -2 | tail -1)
          if [ -z "$PREV_TAG" ]; then
            NOTES="Initial release"
          else
            NOTES=$(git log "${PREV_TAG}..HEAD" --oneline --no-merges --pretty=format:"- %s" | head -50)
          fi
          echo "$NOTES" > /tmp/release-notes.md

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STABLE: ${{ needs.classify.outputs.stable }}
        run: |
          if [ "$STABLE" = true ]; then
            PRERELEASE_FLAG="--prerelease=false"
          else
            PRERELEASE_FLAG="--prerelease"
          fi
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release edit "$GITHUB_REF_NAME" \
              --title "$GITHUB_REF_NAME" \
              --notes-file /tmp/release-notes.md \
              --draft=false \
              $PRERELEASE_FLAG \
              --verify-tag
            gh release upload "$GITHUB_REF_NAME" \
              artifacts/*.tar.gz artifacts/checksums.txt \
              --clobber
          else
            gh release create "$GITHUB_REF_NAME" \
              --title "$GITHUB_REF_NAME" \
              --notes-file /tmp/release-notes.md \
              $PRERELEASE_FLAG \
              --verify-tag \
              artifacts/*.tar.gz artifacts/checksums.txt
          fi

  # ───────────────────────────────────────────────
  # Update Homebrew formula
  # ───────────────────────────────────────────────
  update-homebrew:
    name: Homebrew
    if: needs.classify.outputs.stable == 'true'
    needs: [classify, github-release]
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: cli-*
          path: artifacts
          merge-multiple: true

      - name: Compute checksums
        id: sha
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          for platform in darwin-arm64 darwin-x86_64 linux-arm64 linux-x86_64; do
            FILE="artifacts/a3s-search-${VERSION}-${platform}.tar.gz"
            if [ -f "$FILE" ]; then
              HASH=$(sha256sum "$FILE" | awk '{print $1}')
              KEY=$(echo "$platform" | tr '-' '_')
              echo "${KEY}=$HASH" >> "$GITHUB_OUTPUT"
            fi
          done

      - name: Checkout homebrew-tap
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          repository: A3S-Lab/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Update formula
        run: |
          VERSION="${{ steps.sha.outputs.version }}"
          SHA_DARWIN_ARM64="${{ steps.sha.outputs.darwin_arm64 }}"
          SHA_DARWIN_X86_64="${{ steps.sha.outputs.darwin_x86_64 }}"
          SHA_LINUX_ARM64="${{ steps.sha.outputs.linux_arm64 }}"
          SHA_LINUX_X86_64="${{ steps.sha.outputs.linux_x86_64 }}"

          cat > homebrew-tap/Formula/a3s-search.rb << EOF
          class A3sSearch < Formula
            desc "Embeddable metasearch CLI with browser, HTTP/RSS, and native API sources"
            homepage "https://github.com/A3S-Lab/Search"
            version "${VERSION}"
            license "MIT"

            on_macos do
              on_arm do
                url "https://github.com/A3S-Lab/Search/releases/download/v${VERSION}/a3s-search-${VERSION}-darwin-arm64.tar.gz"
                sha256 "${SHA_DARWIN_ARM64}"
              end
              on_intel do
                url "https://github.com/A3S-Lab/Search/releases/download/v${VERSION}/a3s-search-${VERSION}-darwin-x86_64.tar.gz"
                sha256 "${SHA_DARWIN_X86_64}"
              end
            end

            on_linux do
              on_arm do
                url "https://github.com/A3S-Lab/Search/releases/download/v${VERSION}/a3s-search-${VERSION}-linux-arm64.tar.gz"
                sha256 "${SHA_LINUX_ARM64}"
              end
              on_intel do
                url "https://github.com/A3S-Lab/Search/releases/download/v${VERSION}/a3s-search-${VERSION}-linux-x86_64.tar.gz"
                sha256 "${SHA_LINUX_X86_64}"
              end
            end

            def install
              bin.install "a3s-search"
              (pkgshare/"skills").install "skills/a3s-search"
            end

            test do
              assert_match "a3s-search", shell_output("#{bin}/a3s-search --version")
              assert_path_exists pkgshare/"skills/a3s-search/SKILL.md"
              assert_path_exists pkgshare/"skills/a3s-search/agents/openai.yaml"
            end
          end
          EOF

      - name: Commit and push
        working-directory: homebrew-tap
        env:
          VERSION: ${{ steps.sha.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/a3s-search.rb
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "a3s-search: update to ${VERSION}"
          git push

  release-outcome:
    name: Verify release outcome
    if: always() && !cancelled()
    needs: [classify, github-release, update-homebrew]
    runs-on: ubuntu-latest
    steps:
      - name: Require expected release outputs
        env:
          STABLE: ${{ needs.classify.outputs.stable }}
          GITHUB_RELEASE_RESULT: ${{ needs.github-release.result }}
          HOMEBREW_RESULT: ${{ needs.update-homebrew.result }}
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_RELEASE_RESULT" = success
          if [ "$STABLE" = true ]; then
            test "$HOMEBREW_RESULT" = success
          else
            test "$HOMEBREW_RESULT" = skipped
          fi
