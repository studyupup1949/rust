[package]
name = "aamva"
version = "0.0.0"
edition = "2021"
description = "AAMVA DL/ID PDF417 barcode parser (US / Canadian driver's licenses). Name reserved; implementation in progress."
license = "MIT OR Apache-2.0"
repository = "https://github.com/wearekesk/kyc-rs"
readme = "README.md"
keywords = ["kyc", "identity"]
categories = ["parser-implementations"]
