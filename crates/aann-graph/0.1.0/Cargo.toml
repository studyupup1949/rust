# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "aann-graph"
version = "0.1.0"
build = false
exclude = [
    "*.ipynb",
    "profile.svg",
    "_static/",
    "arena/",
    ".github/",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Approximate all-nearest-neighbour search over neighbourhood graphs, SIMD-accelerated, with optional Python bindings."
homepage = "https://github.com/schlegelp/aann"
readme = "README.md"
keywords = [
    "nearest-neighbor",
    "ann",
    "delaunay",
    "spatial",
    "graph",
]
categories = [
    "algorithms",
    "science",
    "mathematics",
    "data-structures",
]
license = "GPL-3.0-or-later"
repository = "https://github.com/schlegelp/aann"

[features]
default = []
python = [
    "dep:pyo3",
    "dep:numpy",
]

[lib]
name = "aann"
crate-type = [
    "cdylib",
    "rlib",
]
path = "src/lib.rs"

[dependencies.ndarray]
version = "0.17"

[dependencies.numpy]
version = "0.29"
optional = true

[dependencies.pyo3]
version = "0.29.0"
features = ["abi3-py310"]
optional = true

[profile.release]
lto = "fat"
codegen-units = 1
