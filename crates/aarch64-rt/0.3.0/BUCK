load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "aarch64_rt",
    srcs = glob(["**/*.rs"]),
    crate = "aarch64_rt",
    crate_root = "src/lib.rs",
    edition = "2024",
    features = ["default"],

    visibility = ["PUBLIC"],
)
