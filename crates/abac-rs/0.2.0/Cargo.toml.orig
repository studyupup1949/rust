[package]
name = "abac-rs"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version = "1.80"
license.workspace = true
description = "Generic Attribute-Based Access Control (ABAC) evaluation engine with hbac-rs performance characteristics"
repository.workspace = true
keywords = ["abac", "access-control", "authorization", "security"]
categories = ["authentication"]

[dependencies]
acls-rs = { workspace = true, default-features = false }
fastbloom = { workspace = true, optional = true }
smallvec = { version = "1.13", optional = true }
ipnetwork = "0.20"
serde = { version = "1.0", optional = true }
thiserror = { workspace = true }
log = { workspace = true }

# For WASM: use lru without hashbrown (fallback to std HashMap, no runtime randomness)
# Also use ahash with no-rng to avoid std::time dependency
[target.'cfg(target_arch = "wasm32")'.dependencies]
lru = { workspace = true }
ahash = { workspace = true, features = ["no-rng", "std"] }
getrandom = { version = "0.3", features = ["wasm_js"] }

# For native: use lru with hashbrown for better performance
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
lru = { workspace = true, default-features = true }
ahash = { workspace = true, features = ["compile-time-rng", "std"] }

[features]
default = ["bloom"]
bloom = ["dep:fastbloom", "dep:smallvec"]
serde = ["dep:serde", "ipnetwork/serde", "acls-rs/serde"]

[[example]]
name = "environmental_sensors_toml"
required-features = ["serde"]

[[example]]
name = "environmental_sensors_yaml"
required-features = ["serde"]

[[example]]
name = "environmental_sensors_yaml_file"
required-features = ["serde"]

[[bench]]
name = "scaling"
harness = false

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
