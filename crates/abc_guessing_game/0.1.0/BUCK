load("@buckal//:wrapper.bzl", "rust_binary")

rust_binary(
    name = "abc_guessing_game",
    srcs = glob(["**/*.rs"]),
    crate = "abc_guessing_game",
    crate_root = "src/main.rs",
    edition = "2021",

    visibility = ["PUBLIC"],
)
