[package]
name = "abineo-messaging"
description = "Shared structs and enums for internal messaging service."
version = "1.0.0"
edition = "2021"
license = "ISC"

[dependencies]
email_address = "0.2"
serde = { version = "1", features = ["derive"] }
thiserror = "1"
