# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "abitious-decmpfs"
version = "0.1.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The abitious pressed-data section ABI: build, parse, and locate a compressed-hybrid .node section (mirror of decmpfs's reader)."
homepage = "https://github.com/SocketDev/abitious"
readme = false
keywords = [
    "napi",
    "addon",
    "compression",
    "decmpfs",
    "zstd",
]
categories = [
    "compression",
    "development-tools",
]
license = "MIT"
repository = "https://github.com/SocketDev/abitious"

[features]
default = []
resign = ["dep:apple-codesign"]

[lib]
name = "abitious_decmpfs"
path = "src/lib.rs"

[[example]]
name = "gen_corpus"
path = "examples/gen_corpus.rs"

[[test]]
name = "interop_corpus"
path = "tests/interop_corpus.rs"

[[test]]
name = "roundtrip"
path = "tests/roundtrip.rs"

[dependencies.sha2]
version = "=0.10.9"

[dependencies.zstd]
version = "=0.13.3"

[dev-dependencies.proptest]
version = "1"

[target.'cfg(target_os = "macos")'.dependencies.apple-codesign]
version = "0.29"
optional = true
default-features = false

[target."cfg(unix)".dependencies.libc]
version = "0.2"

[target."cfg(windows)".dependencies.windows-sys]
version = "0.59"
features = [
    "Win32_Foundation",
    "Win32_System_LibraryLoader",
    "Win32_Security",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
]

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(coverage)",
    "cfg(coverage_nightly)",
]
