# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "abitious-producer"
version = "0.1.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The abitious host producer: compress a real .node addon and inject + ad-hoc re-sign it into a generic stub's SMOL/__PRESSED_DATA section, emitting a self-loading hybrid .node."
homepage = "https://github.com/SocketDev/abitious"
readme = false
keywords = [
    "napi",
    "addon",
    "compression",
    "producer",
    "codesign",
]
categories = [
    "development-tools",
    "command-line-utilities",
]
license = "MIT"
repository = "https://github.com/SocketDev/abitious"

[lib]
name = "abitious_producer"
path = "src/lib.rs"

[[bin]]
name = "abitious-producer"
path = "src/main.rs"

[[test]]
name = "cli"
path = "tests/cli.rs"

[[test]]
name = "e2e"
path = "tests/e2e.rs"

[[test]]
name = "install_e2e"
path = "tests/install_e2e.rs"

[dependencies.abitious-decmpfs]
version = "0.1.0"
features = ["resign"]

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(coverage)",
    "cfg(coverage_nightly)",
]
