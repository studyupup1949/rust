# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "abitious-stub"
version = "0.1.0"
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The generic abitious self-extracting trampoline: a cdylib whose napi_register_module_v1 self-extracts the real addon from its own SMOL/__PRESSED_DATA section and forwards registration to it."
homepage = "https://github.com/SocketDev/abitious"
readme = false
keywords = [
    "napi",
    "addon",
    "trampoline",
    "dlopen",
    "self-extracting",
]
categories = ["development-tools"]
license = "MIT"
repository = "https://github.com/SocketDev/abitious"

[lib]
name = "abitious_stub"
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies.abitious-decmpfs]
version = "0.1.0"

[target."cfg(unix)".dependencies.libc]
version = "0.2"

[target."cfg(windows)".dependencies.windows-sys]
version = "0.59"
features = [
    "Win32_Foundation",
    "Win32_System_LibraryLoader",
    "Win32_Security",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
]

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(coverage)",
    "cfg(coverage_nightly)",
]
