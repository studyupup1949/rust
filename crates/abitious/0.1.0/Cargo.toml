# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "abitious"
version = "0.1.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The abitious `abi` CLI: cargo-build a napi cdylib for the host triple, resolve its .node artifact, and optionally compress it into a self-loading hybrid .node via the abitious producer."
homepage = "https://github.com/SocketDev/abitious"
readme = false
keywords = [
    "napi",
    "addon",
    "cli",
    "compression",
    "cargo",
]
categories = [
    "development-tools",
    "command-line-utilities",
]
license = "MIT"
repository = "https://github.com/SocketDev/abitious"

[[bin]]
name = "abi"
path = "src/main.rs"

[[test]]
name = "cli"
path = "tests/cli.rs"

[[test]]
name = "e2e"
path = "tests/e2e.rs"

[dependencies.abitious-decmpfs]
version = "0.1.0"

[dependencies.abitious-producer]
version = "0.1.0"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(coverage)",
    "cfg(coverage_nightly)",
]
