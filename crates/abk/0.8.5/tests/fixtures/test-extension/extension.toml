[extension]
id = "test-extension"
name = "Test Extension"
version = "0.1.0"
api_version = "0.3.0"
description = "A minimal test extension for verifying the extension loader"
authors = ["ABK Team"]

[lib]
kind = "rust"
path = "test_extension.wasm"

[capabilities]
lifecycle = true
provider = false
tools = false
context = false
