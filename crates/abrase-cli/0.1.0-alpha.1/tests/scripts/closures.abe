fn main() -> Int {
  let no_cap = |x| x + x;
  let r1 = no_cap(7);

  let x = 5;
  let one_cap = |y| x + y;
  let r2 = one_cap(3);

  let a = 1;
  let b = 2;
  let multi_cap = |c| a + b + c;
  let r3 = multi_cap(3);

  r1 + r2 + r3
}
