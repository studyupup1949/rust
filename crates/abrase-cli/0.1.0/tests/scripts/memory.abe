fn through_ref(x: &Int) -> Int {
  *x + *x
}

fn main() -> Int {
  let n = 10;
  let doubled = through_ref(&n);
  let val = region {
    let cell = Shared(doubled);
    *cell
  };
  let s = "moved";
  let t = s;
  {
    let inner = "scoped";
    inner;
  };
  val + n
}
