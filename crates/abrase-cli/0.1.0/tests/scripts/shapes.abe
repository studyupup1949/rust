type Pt = { x: Int, y: Int }

fn dist_sq(p: Pt) -> Int {
  p.x * p.x + p.y * p.y
}

fn main() -> Int {
  let a: Pt = Pt { x: 1, y: 2 };
  let b: Pt = Pt { x: 3, y: 4 };
  let c: Pt = Pt { x: 5, y: 12 };
  let pts = [a, b, c];
  dist_sq(pts[2])
}
