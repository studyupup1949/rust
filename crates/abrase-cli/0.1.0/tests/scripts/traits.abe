trait Doubler {
  fn double(self) -> Int { 0 }
}

impl Doubler for Int {
  fn double(self) -> Int {
    self * 2
  }
}

fn id<T>(x: T) -> T { x }

fn main() -> Int {
  let flag = id(true);
  let n = id(42);
  let d = (5).double();
  if flag { n + d } else { d }
}
