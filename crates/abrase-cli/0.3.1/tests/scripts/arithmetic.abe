fn fib(n: Int) -> Int {
  if n <= 1 {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
}

fn sum_to(n: Int) -> Int {
  let mut sum = 0;
  let mut i = 1;
  while i <= n {
    sum = sum + i;
    i = i + 1
  };
  sum
}

fn main() -> Int {
  fib(10) + sum_to(10)
}
