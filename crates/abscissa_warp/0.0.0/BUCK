load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "abscissa_warp",
    srcs = glob(["**/*.rs"]),
    crate = "abscissa_warp",
    crate_root = "src/lib.rs",
    edition = "2015",

    visibility = ["PUBLIC"],
)
