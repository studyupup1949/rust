load("@buckal//:wrapper.bzl", "rust_binary")

rust_binary(
    name = "absl_duration",
    srcs = glob(["**/*.rs"]),
    crate = "absl_duration",
    crate_root = "src/main.rs",
    edition = "2021",

    visibility = ["PUBLIC"],
)
