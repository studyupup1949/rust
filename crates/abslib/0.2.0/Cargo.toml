# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "abslib"
version = "0.2.0"
build = false
exclude = [
    "CLAUDE.md",
    ".gitignore",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Reusable async networking primitives for single-task protocol drivers: egress backpressure, per-kind concurrency ceilings and rate pacing, and a shared refresh/single-flight registry"
readme = "README.md"
keywords = [
    "tokio",
    "backpressure",
    "networking",
    "async",
]
categories = [
    "asynchronous",
    "network-programming",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/debdattabasu/abslib"

[lib]
name = "abslib"
path = "src/lib.rs"

[dependencies.arc-swap]
version = "1"

[dependencies.tokio]
version = "1"
features = [
    "sync",
    "time",
]
default-features = false

[dev-dependencies.tokio]
version = "1"
features = [
    "rt",
    "rt-multi-thread",
    "macros",
    "sync",
    "time",
    "test-util",
]
