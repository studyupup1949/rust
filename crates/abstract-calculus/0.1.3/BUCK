load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "absal",
    srcs = glob(["**/*.rs"]),
    crate = "absal",
    crate_root = "src/lib.rs",
    edition = "2015",

    visibility = ["PUBLIC"],
)
