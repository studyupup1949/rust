load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "accelerate_src",
    srcs = glob(["**/*.rs"]),
    crate = "accelerate_src",
    crate_root = "src/lib.rs",
    edition = "2015",

    visibility = ["PUBLIC"],
)
