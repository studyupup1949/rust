load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "acceptarium",
    srcs = glob(["**/*.rs"]),
    crate = "acceptarium",
    crate_root = "src/lib.rs",
    edition = "2024",
    features = ["default"],

    visibility = ["PUBLIC"],
)
