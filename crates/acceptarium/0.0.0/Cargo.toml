# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "acceptarium"
version = "0.0.0"
authors = ["Caleb Maclennan <caleb@alerque.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "CLI tool to facilitate digitized receipt handling in plain text accounting workflows"
homepage = "https://codeberg.org/plaintextaccounting/acceptarium"
readme = "README.md"
license = "AGPL-3.0-only"
repository = "https://codeberg.org/plaintextaccounting/acceptarium"
resolver = "2"

[features]
cli = []
default = []

[lib]
name = "acceptarium"
crate-type = [
    "rlib",
    "cdylib",
]
path = "src/lib.rs"

[[bin]]
name = "acceptarium"
path = "src/bin/acceptarium.rs"
required-features = ["cli"]

[dependencies]

[profile.release]
lto = true
