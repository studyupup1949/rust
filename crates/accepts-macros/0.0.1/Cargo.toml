# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "accepts-macros"
version = "0.0.1"
authors = ["Mikou"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Rust toolkit for composing synchronous and asynchronous acceptor pipelines"
readme = "README.md"
license = "MIT OR Apache-2.0"
repository = "https://github.com/accepts-rs/accepts"

[features]
codegen = ["accepts-codegen"]
internal-codegen = ["accepts-codegen"]

[lib]
name = "accepts_macros"
path = "src/lib.rs"
proc-macro = true

[dependencies.accepts-codegen]
version = "0.0.1"
optional = true

[dependencies.proc-macro2]
version = "1"
default-features = false
