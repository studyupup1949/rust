load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "accepts",
    srcs = glob(["**/*.rs"]),
    crate = "accepts",
    crate_root = "src/lib.rs",
    edition = "2024",
    features = ["default"],

    visibility = ["PUBLIC"],
)
