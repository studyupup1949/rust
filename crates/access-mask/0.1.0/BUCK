load("@buckal//:wrapper.bzl", "rust_binary")

rust_binary(
    name = "access_mask",
    srcs = glob(["**/*.rs"]),
    crate = "access_mask",
    crate_root = "src/main.rs",
    edition = "2024",

    visibility = ["PUBLIC"],
)
