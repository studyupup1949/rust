# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "acevo-shared-memory"
version = "0.2.0"
authors = ["Domenico Mancini"]
build = "build.rs"
exclude = [
    "AGENTS.md",
    ".vscode/",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A library to easily access Assetto Corsa Evo shared memory."
readme = "README.md"
keywords = [
    "windows",
    "memory",
    "shared",
    "ipc",
    "acevo",
]
categories = [
    "os::windows-apis",
    "api-bindings",
]
license = "MIT"
repository = "https://github.com/dSyncro/acevo-shared-memory"

[features]
default = []
serde = [
    "dep:serde",
    "dep:serde_arrays",
]

[lib]
name = "acevo_shared_memory"
path = "src/lib.rs"

[[example]]
name = "dashboard"
path = "examples/dashboard.rs"

[[example]]
name = "monitor"
path = "examples/monitor.rs"

[dependencies.serde]
version = "1"
features = ["derive"]
optional = true

[dependencies.serde_arrays]
version = "0.2"
optional = true

[dependencies.strum]
version = "0.28"
features = ["derive"]

[dependencies.win-shared-memory]
version = "0.1"

[dev-dependencies.ratatui]
version = "0.30"

[build-dependencies.bindgen]
version = "0.72"
