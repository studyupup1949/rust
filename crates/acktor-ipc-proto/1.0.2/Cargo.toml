# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "acktor-ipc-proto"
version = "1.0.2"
authors = ["Chao Gu"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Interprocess communication protocol for the acktor actor framework"
readme = "README.md"
categories = ["asynchronous"]
license = "MIT"
repository = "https://github.com/asymmetry/acktor"
resolver = "2"

[lib]
name = "acktor_ipc_proto"
path = "src/lib.rs"

[dependencies.bytes]
version = "1.10"

[dependencies.prost]
version = "0.14"

[target.'cfg(target_arch = "wasm32")'.dependencies.ahash]
version = "0.8"
