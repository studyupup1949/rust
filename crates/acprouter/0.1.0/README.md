# acprouter

`acprouter` is intended for ACP routing and dispatch primitives around agent runtimes and transport
boundaries.

At the moment this crate is intentionally minimal and does not yet provide a Rust implementation of
the full router surface.

Project source:

- <https://github.com/vcoderun/acpkit>
