# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "action-gate-core"
version = "0.1.0"
authors = ["The action-gate Authors"]
build = false
include = [
    "src/**",
    "Cargo.toml",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The action-gate Gate: a pure, deterministic decision engine over a pluggable, ordered Check registry, with a stable config hash and optional common checks."
homepage = "https://github.com/stagecraft-ing/action-gate"
readme = "README.md"
keywords = [
    "policy",
    "gate",
    "governance",
    "decision",
    "deterministic",
]
categories = [
    "algorithms",
    "data-structures",
]
license = "Apache-2.0"
repository = "https://github.com/stagecraft-ing/action-gate"
resolver = "2"

[package.metadata.action-gate]
spec = "000-action-gate-bootstrap"

[features]
checks-common = ["dep:regex"]
default = []

[lib]
name = "action_gate_core"
path = "src/lib.rs"

[dependencies.action-gate-types]
version = "0.1.0"

[dependencies.canonical-keysort-json]
version = "0.1"

[dependencies.hex]
version = "0.4"

[dependencies.regex]
version = "1"
optional = true

[dependencies.serde]
version = "1"
features = ["derive"]

[dependencies.serde_json]
version = "1"

[dependencies.sha2]
version = "0.10"

[lints.rust]
unsafe_code = "forbid"
