name: Bad Action
description: Example action manifest with invalid YAML.

inputs:
  format:
    description: Receipt format: text, json, or markdown.

runs:
  using: composite
  steps:
    - name: Say hello
      shell: bash
      run: echo "hello"

