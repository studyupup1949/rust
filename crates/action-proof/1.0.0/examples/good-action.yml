name: Good Action
description: Example action manifest that passes action-proof.
author: Wildmason

branding:
  icon: check-circle
  color: green

inputs:
  format:
    description: "Receipt format: text, json, or markdown."
    required: false
    default: text

runs:
  using: composite
  steps:
    - name: Say hello
      shell: bash
      run: echo "hello"

