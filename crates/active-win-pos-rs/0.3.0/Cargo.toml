# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "active-win-pos-rs"
version = "0.3.0"
authors = ["Dmitry Malkov <dmitry.malkov@live.com>"]
description = "Get position and size of the active window on Windows and MacOS"
keywords = ["window", "active", "current", "position"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/dimusic/active-win-pos-rs"
resolver = "2"
[target."cfg(target_os = \"macos\")".dependencies.appkit-nsworkspace-bindings]
version = "0.1.0"

[target."cfg(target_os = \"macos\")".dependencies.core-foundation]
version = "0.9"

[target."cfg(target_os = \"macos\")".dependencies.core-graphics]
version = "0.22"
[target."cfg(target_os = \"windows\")".dependencies.winapi]
version = "0.3.9"
features = ["winuser"]
