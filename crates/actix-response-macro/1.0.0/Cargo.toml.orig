[package]
name = "actix-response-macro"
description = "Helper crate for `actix-response` which defines the proc macro used."
license = "GPL-3.0-only"
version = "1.0.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0.94"
quote = "1.0.40"
syn = { version = "2.0.100", features = ["full"] }
