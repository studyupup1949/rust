# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "actix-web-schema"
version = "0.4.0"
authors = ["killf <killf@foxmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Schema-based development tools for actix-web with service and response macros"
readme = "README.md"
keywords = [
    "actix",
    "actix-web",
    "web",
    "schema",
    "macro",
]
categories = [
    "web-programming",
    "api-bindings",
]
license = "Apache-2.0"
repository = "https://github.com/killf/actix-web-schema"

[lib]
name = "actix_web_schema"
path = "src/lib.rs"

[dependencies.actix-web-schema-macro]
version = "0.4.0"

[dev-dependencies.actix-web]
version = "4.13"

[dev-dependencies.serde]
version = "1.0"
features = ["serde_derive"]

[dev-dependencies.serde_json]
version = "1.0"
