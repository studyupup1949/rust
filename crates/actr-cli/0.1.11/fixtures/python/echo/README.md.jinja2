# {{PROJECT_NAME}} (echo_demo)

Python echo demo generated by `actr init --language python`.

## Setup

```bash
pip install framework_codegen_python actr
```

## Generate code

```bash
actr gen --lang python --input=proto --output=generated --no-scaffold
```

## Run

```bash
cd server
python server.py --actr-toml Actr.toml
```

```bash
cd client
python client.py --actr-toml Actr.toml
```
