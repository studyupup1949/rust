// ACTR: mutable scaffold
// User-owned RPC implementation. Keep Generated/ immutable and customize this file.

import Actr
import Foundation

public final class {{SERVICE_NAME}}HandlerImpl: {{HANDLER_NAME}} {
    // Use targetType with ctx.discover(targetType:) when forwarding local RPCs
    // to a remote dependency declared in manifest.toml.
    private let targetType: ActrType?

    public init(targetType: ActrType? = nil) {
        self.targetType = targetType
    }

{{#each METHODS}}
    public func {{swift_name}}(
        req: {{input_type}},
        ctx: any ActrContext
    ) async throws(ActrError) -> {{output_type}} {
        throw ActrError.NotImplemented(msg: "{{name}} is not implemented")
    }

{{/each}}
}
