# {{PROJECT_NAME}}

Empty Actrium Swift app shell.

## Next steps

1. Add local proto definitions under `protos/local/local.proto`.
2. Run `actr gen -l swift`.
3. Wire the generated client code into `{{PROJECT_NAME_PASCAL}}/ContentView.swift`.
4. Run `xcodegen generate` after editing `project.yml`.
