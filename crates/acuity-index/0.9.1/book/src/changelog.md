# Changelog

The canonical project changelog now lives at the repository root in
[`CHANGELOG.md`](../../CHANGELOG.md).

GitHub release notes are generated from that file so the release metadata and
in-repo changelog stay in sync.
