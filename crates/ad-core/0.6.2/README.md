# ad-core

> **DEPRECATED**: This crate has been replaced by [`epics-rs`](https://crates.io/crates/epics-rs).

```toml
[dependencies]
epics-rs = { version = "0.8", features = ["ad"] }
```

See https://github.com/epics-rs/epics-rs for documentation.
