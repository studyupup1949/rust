mutation CreateUser($input: UserCreate!) {
    createUser(input: $input) {
        user {
            id
            email
            name
            userType
            createdAt
            deleted
        }
        generatedApiKey
    }
}
