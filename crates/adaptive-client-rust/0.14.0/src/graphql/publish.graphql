mutation PublishCustomRecipe(
    $file: Upload!
    $name: String
    $key: String
    $project: IdOrKey!
) {
    createCustomRecipe(
        input: { name: $name, key: $key }
        project: $project
        file: $file
    ) {
        id
        key
    }
}
