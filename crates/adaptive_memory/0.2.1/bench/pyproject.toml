[project]
name = "locomo-bench"
version = "0.1.0"
description = "LoCoMo benchmark harness for adaptive_memory"
requires-python = ">=3.11"
dependencies = [
    "nltk>=3.8",
]

[dependency-groups]
dev = []
