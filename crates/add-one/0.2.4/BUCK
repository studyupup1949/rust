load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "add_one",
    srcs = glob(["**/*.rs"]),
    crate = "add_one",
    crate_root = "src/lib.rs",
    edition = "2015",

    visibility = ["PUBLIC"],
)
