load("@buckal//:wrapper.bzl", "rust_library")

rust_library(
    name = "add_ext",
    srcs = glob(["**/*.rs"]),
    crate = "add_ext",
    crate_root = "src/lib.rs",
    edition = "2024",
    features = ["default"],

    visibility = ["PUBLIC"],
)
