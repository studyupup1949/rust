# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.56.0"
name = "addended_ordered_map"
version = "0.1.0"
authors = ["Anghelo Carvajal <angheloalf95@gmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Ordered mapping with ranged lookups. Range sizes can be modified after creation."
readme = "README.md"
keywords = [
    "data-structures",
    "map",
    "ordered-mapping",
    "ranged-mapping",
]
categories = ["no-std"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/Decompollaborate/addended_ordered_map"

[features]
default = []
extract_if = []
nightly = []
std = []

[lib]
name = "addended_ordered_map"
path = "src/lib.rs"

[dependencies.polonius-the-crab]
version = "0"
