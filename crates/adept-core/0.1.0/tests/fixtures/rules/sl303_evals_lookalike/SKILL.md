---
name: sl303-evals-lookalike
description: Use when the user asks to test that an identically-sized evals.jsonl file NOT inside an evals/ directory still fires SL303.
---
# SL303 Evals Lookalike

See evals.jsonl (a top-level file, not inside an `evals/` directory) — the
exemption matches by directory name only, so this lookalike still fires.
