Wed 27 Feb 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	New release v0.1.3

	* Cargo.{lock,toml}, README.md and CHANGELOG

	Fix argument types to Validator and OptionalValidator

	* src/validation/mod.rs: Change `Fn(&str)` to `Fn(&String)` and change
	`Fn(Option<&str>)` to `Fn(&Option<String>)` so that validators can check
	`String` or `Option<String>` as general user inputs

Fri 31 Jan 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	Update package configuration

	* Cargo.toml: Update exclude targets

	Delete .gitlab-ci.yml

	* .gitlab-ci.yaml: Deleted

	Remove pull_request event from the builder workflow

	Add a CI badge in README.md

	Split .woodpecker.yaml into files in .woodpecker dir

	* .woodpecker/{builder,pipeline}.yaml

Thu 30 Jan 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	Add .woodpecker.yaml as an experiment

	* .woodpecker.yaml
	* Dockerfile.builder

	Update Makefile

	* Makefile: replace colon with a dash (e.g. make build-debug), remove
	unnecessary runner target
	* .gitlab-ci.yml, .env.ci.sample: Deleted

Sat 25 Jan 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	New release v0.1.3

	* Cargo.{lock,toml}, README.md and CHANGELOG

	Marked as 'experimental' status (at least while v0.1.x)

	Update dependency crates (lazy_static and strfmt)

	* Cargo.{lock,toml}

	Add contains_any and helper validators by using it

	* New validators
		* contains_any
	  * contains_any_digits
	  * contains_any_lower_letters
	  * contains_any_upper_letters

Fri 24 Jan 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add contains_only and contains_only_alphanumeric_chars validators

Sun 19 Jan 2025 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add Serialize derive to Feedback and Message
	* Add serde as a dependency

Sat 26 Aug 2023 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Reduce the use of String and Option<String> in structs. So that
	users can reuse built validator function without creating unnecessary string
	objects again
	* Create Validator and OptionalValidator types
	* Remove public access to ValidatinResult type

Wed 14 Sep 2022 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Fix warnings by checks of the latest clippy (stable)

Sun 06 Jun 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Change error messages for max/min validations (contain -> have)
	* Add a new validation method "not_contain_if_given"

Sat 29 May 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add a new validation method "contains_if_given"

Sat 22 May 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add a new validation method "contains_if_present"

Thu 20 May 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add a new validation method "contains"

Wed 19 May 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* New release v0.1.2
	* Fix bugs in PartialEq for Error, Feedback and Message struct
	* Split modules into separated files (error, feedback and message)
	* Add .renovaterc.json (dependencies check by renovate-runner)

Sun 02 May 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add docs (pages) for adequate.yasha.rs
	* Update base image for CI (use them from registry.gitlab.com)
	* Remove .tool

Wed 06 Jan 2021 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Change the default branch as `trunk`

Fri 21 Nov 2020 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* New release v0.1.1
	* Add a new length validation methods "{max,min}_if_present"

Mon 16 Nov 2020 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Add a new length validation method "within"

Fri 16 Oct 2020 Yasha <yasuhiro.asaka@grauwoelfchen.net>
	* Initial release v0.1.0
