# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "adhoc_audio"
version = "0.1.1"
authors = ["k. Collin DaCosta <khadeem.dacosta@gmail.com>"]
exclude = ["resources/*"]
description = "A very basic compression codec for audio, written in pure rust"
keywords = ["audio", "compression", "codec", "adhoc"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/K-C-DaCosta/adhoc_audio"
resolver = "2"
[dependencies.bincode]
version = "1.3.3"

[dependencies.clap]
version = "2.33.3"
optional = true

[dependencies.rand]
version = "^0.8.0"
optional = true

[dependencies.serde]
version = "1.0.130"
features = ["derive"]

[features]
cli = ["clap", "rand"]
default = []
