//! Coverage of the crate's `generate-html` documentation module under
//! `docs/modules/generate-html/`, from the CLI's point of view.
//!
//! These pages describe generating HTML5 with `adoc`. This crate verifies the
//! command-line invocations they show; the `asciidoc-html5` crate verifies the
//! API invocations, and the sdd tool merges the two. The module grows a page
//! per topic.

mod custom_stylesheet;
mod index;
