//! Coverage of Asciidoctor's `html-backend` documentation module under
//! `ref/asciidoctor/docs/modules/html-backend/`, from the CLI's point of view.
//!
//! These pages document Asciidoctor's default `html5` backend — the output
//! format `adoc` produces. This crate verifies the command-line invocations
//! each page shows; the module grows a page per topic. The `asciidoc-html5`
//! crate verifies the API-level behavior, and the sdd tool merges the two.

mod index;
