# tree-sitter-toml

Same code as
[tree-sitter/tree-sitter-toml](https://github.com/tree-sitter/tree-sitter-toml),
but build with up to date (as of 2025/10/7) tree-sitter-cli.

Availible on crates.io as [`adot-tree-sitter-toml`](https://crates.io/crates/adot-tree-sitter-toml).

Mainly for me, but PR's welcome I guess.
