[package]
name = "adze-concurrency-env-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "Environment-backed concurrency cap policy and parsing utilities."
repository = "https://github.com/EffortlessMetrics/adze"
publish = ["crates-io"]
keywords = ["concurrency", "threading", "caps", "parallel", "rayon"]
categories = ["concurrency"]
homepage = "https://github.com/EffortlessMetrics/adze"
documentation = "https://docs.rs/adze-concurrency-env-core"

[features]
default = []
strict_api = []
strict_docs = []

[dependencies]
adze-concurrency-env-contract-core = { version = "0.1.0", path = "../concurrency-env-contract-core" }

[dev-dependencies]
proptest.workspace = true
