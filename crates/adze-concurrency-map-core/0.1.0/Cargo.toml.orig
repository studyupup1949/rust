[package]
name = "adze-concurrency-map-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "Bounded parallel map utilities with deterministic partition planning."
repository = "https://github.com/EffortlessMetrics/adze"
publish = ["crates-io"]
keywords = ["concurrency", "threading", "caps", "parallel", "rayon"]
categories = ["concurrency"]
homepage = "https://github.com/EffortlessMetrics/adze"
documentation = "https://docs.rs/adze-concurrency-map-core"

[features]
default = []
strict_api = []
strict_docs = []

[dependencies]
adze-concurrency-bounded-map-core = { version = "0.1.0", path = "../concurrency-bounded-map-core" }

[dev-dependencies]
proptest.workspace = true
criterion.workspace = true
adze-concurrency-normalize-core = { version = "0.1.0", path = "../concurrency-normalize-core" }
adze-concurrency-plan-core = { version = "0.1.0", path = "../concurrency-plan-core" }
adze-concurrency-bounded-map-core = { version = "0.1.0", path = "../concurrency-bounded-map-core" }

[[bench]]
name = "bench_map"
harness = false
