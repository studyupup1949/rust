[package]
name = "adze-concurrency-normalize-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "Single-responsibility helpers for normalized concurrency bounds."
repository = "https://github.com/EffortlessMetrics/adze"
publish = ["crates-io"]
keywords = ["concurrency", "threading", "caps", "parallel", "rayon"]
categories = ["concurrency"]
homepage = "https://github.com/EffortlessMetrics/adze"
documentation = "https://docs.rs/adze-concurrency-normalize-core"

[features]
default = []
strict_api = []
strict_docs = []

[dev-dependencies]
proptest.workspace = true
