[package]
name = "adze-concurrency-parse-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "String parsing helpers for concurrency cap configuration values."
repository = "https://github.com/EffortlessMetrics/adze"
publish = ["crates-io"]
keywords = ["concurrency", "threading", "caps", "parallel", "rayon"]
categories = ["concurrency"]
homepage = "https://github.com/EffortlessMetrics/adze"
documentation = "https://docs.rs/adze-concurrency-parse-core"

[features]
default = []
strict_api = []
strict_docs = []

[dev-dependencies]
proptest.workspace = true
