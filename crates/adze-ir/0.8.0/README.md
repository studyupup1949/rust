# adze-ir

`adze-ir` is the shared Intermediate Representation layer for the `adze` parser
pipeline. It contains shared grammar data structures and helpers used by core table
and parser generation crates.
