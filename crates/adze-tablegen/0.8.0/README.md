# adze-tablegen

`adze-tablegen` provides table generation and compression utilities used by the
`adze` pipeline. It converts parser IR and GLR artifacts into runtime-ready
parse tables and related generated outputs.
